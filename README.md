---

# 🛍️ Web-Larёk — Интернет-магазин для разработчиков

**Web-Larёk** — учебный проект интернет-магазина, выполненный на архитектуре MVP. Позволяет просматривать каталог товаров, добавлять их в корзину и оформлять заказ.

---

# 🏗️ Архитектура проекта

Проект построен на паттерне **MVP (Model-View-Presenter)**.

Структура:

```
src/
├── models/             # Модели данных
├── views/              # Представления (UI-слой)
├── presenters/         # Презентеры (бизнес-логика)
├── components/         # UI-компоненты
├── utils/              # Вспомогательные функции
└── types/              # Интерфейсы и типы
```

---

# 📦 Данные

В приложении используются две основные сущности:

## 🎯 Интерфейс товара

```ts
interface IProductItem {
  id: string;
  title: string;
  description: string;
  price: number | null;
  image: string;
  category: string;
}
```

**Назначение:** описывает товар в каталоге.

---

## 👤 Интерфейс покупателя

```ts
interface ICustomerData {
  payment: "card" | "cash" | "";
  email: string;
  phone: string;
  address: string;
}
```

**Назначение:** описывает данные для оформления заказа.

---

## 📦 Интерфейс заказа

```ts
interface IOrderData extends ICustomerData {
  items: IProductItem[];
}
```

**Назначение:** данные, отправляемые на сервер при создании заказа.

---

# 🧠 Модели данных (Models)

Модели отвечают за хранение и управление данными. Ниже описаны все модели, поля, методы и ответственность.

---

# 1. **ProductsModel — Каталог товаров**

### 🎯 Назначение

Отвечает за хранение списка товаров и выбранного товара для детального просмотра.

---

### 🧩 Поля

| Поле               | Тип                    | Описание                       |
| ------------------ | ---------------------- | ------------------------------ |
| `private products` | `IProductItem[]`       | Массив всех товаров            |
| `private selected` | `IProductItem \| null` | Товар, выбранный для просмотра |

---

### 🛠️ Методы

| Метод                   | Параметры                  | Возвращает                  | Описание                   |
| ----------------------- | -------------------------- | --------------------------- | -------------------------- |
| `setProducts(products)` | `products: IProductItem[]` | `void`                      | Сохранить массив товаров   |
| `getProducts()`         | —                          | `IProductItem[]`            | Получить список товаров    |
| `getById(id)`           | `id: string`               | `IProductItem \| undefined` | Найти товар по id          |
| `setSelected(product)`  | `product: IProductItem`    | `void`                      | Установить выбранный товар |
| `getSelected()`         | —                          | `IProductItem \| null`      | Получить выбранный товар   |

---

# 2. **BasketModel — Корзина**

### 🎯 Назначение

Хранит товары, выбранные покупателем, и предоставляет методы для работы с корзиной.

---

### 🧩 Поля

| Поле            | Тип              | Описание                      |
| --------------- | ---------------- | ----------------------------- |
| `private items` | `IProductItem[]` | Товары, добавленные в корзину |

---

### 🛠️ Методы

| Метод                 | Параметры               | Возвращает       | Описание                |
| --------------------- | ----------------------- | ---------------- | ----------------------- |
| `getItems()`          | —                       | `IProductItem[]` | Список товаров корзины  |
| `addItem(product)`    | `product: IProductItem` | `void`           | Добавить товар          |
| `removeItem(product)` | `product: IProductItem` | `void`           | Удалить товар           |
| `clear()`             | —                       | `void`           | Очистить корзину        |
| `getTotal()`          | —                       | `number`         | Суммарная стоимость     |
| `getCount()`          | —                       | `number`         | Количество товаров      |
| `hasItem(id)`         | `id: string`            | `boolean`        | Проверка наличия товара |

---

# 3. **CustomerModel — Данные покупателя**

### 🎯 Назначение

Хранит данные покупателя и выполняет их валидацию.

---

### 🧩 Поля

| Поле              | Тип                      | Описание       |
| ----------------- | ------------------------ | -------------- |
| `private payment` | `"card" \| "cash" \| ""` | Способ оплаты  |
| `private email`   | `string`                 | Email          |
| `private phone`   | `string`                 | Телефон        |
| `private address` | `string`                 | Адрес доставки |

---

### 🛠️ Методы

| Метод           | Параметры                      | Возвращает                    | Описание                 |
| --------------- | ------------------------------ | ----------------------------- | ------------------------ |
| `setData(data)` | `data: Partial<ICustomerData>` | `void`                        | Обновляет выбранные поля |
| `getData()`     | —                              | `ICustomerData`               | Получить все данные      |
| `clear()`       | —                              | `void`                        | Очистить данные          |
| `validate()`    | —                              | `{ [field: string]: string }` | Возвращает ошибки        |

Пример результата валидации:

```ts
{
  email: "Введите email",
  phone: "Укажите номер телефона",
}
```

Поле отсутствует в объекте ошибок — оно валидно.

---

# 🖼️ Представления (Views)

Представления отвечают за отображение данных и работу с DOM. Они ничего не знают о бизнес-логике — ей управляет Presenter.

---

# 1. **ProductView — отображение товаров**

### Назначение

Показывает товары на странице и детальную карточку.

### Методы

- `renderList(products: IProductItem[])`
- `renderDetails(product: IProductItem)`
- `onSelect(id: string)` — обработчик выбора товара

---

# 2. **BasketView — корзина**

### Назначение

Отображение корзины и данных о стоимости.

### Методы

- `render(items: IProductItem[])`
- `updateTotal(total: number)`
- `updateCount(count: number)`

---

# 3. **CustomerFormView — форма покупателя**

### Назначение

Обеспечивает ввод данных покупателя.

### Методы

- `render(data: ICustomerData)`
- `showErrors(errors: object)`
- `onSubmit()` — отправка формы

---

# 🧠 Presenter (бизнес-логика)

Презентер соединяет Модель и View.
Он получает события из View, обновляет модели и инициирует изменения интерфейса.

---

# 🌐 API

| Метод | Endpoint       | Описание        |
| ----- | -------------- | --------------- |
| GET   | `/api/product` | Список товаров  |
| POST  | `/api/order`   | Создание заказа |

---

# 🚀 Быстрый старт

```bash
npm install
npm run start
```

---

# 🛠️ Технический стек

- TypeScript
- SCSS
- HTML5
- EventEmitter
- MVP

---

# 📄 Лицензия

MIT

---
