# 🛍️ Web-Larёk - Интернет-магазин для разработчиков

**Web-Larёk** — современный интернет-магазин товаров для веб-разработчиков с полным циклом покупки, от просмотра каталога до оформления заказа.

## ✨ Особенности

- 🎯 **Интуитивный интерфейс** с модальными окнами и плавными переходами
- 🛒 **Умная корзина** с подсчетом стоимости и количеством товаров
- 💳 **Несколько способов оплаты** (карта, наличные)
- 📱 **Адаптивный дизайн** для всех устройств
- 🔒 **Валидация форм** с понятными сообщениями об ошибках
- ⚡ **Быстрая загрузка** благодаря оптимизированной сборке

## 🏗️ Архитектура

Проект построен по паттерну **MVP (Model-View-Presenter)**:

```
src/
├── components/          # Компоненты UI
│   ├── base/           # Базовые компоненты
│   └── ...             # Специфические компоненты
├── models/             # Модели данных
├── views/              # Представления
├── presenters/         # Презентеры (бизнес-логика)
├── utils/              # Вспомогательные функции
│   ├── constants.ts    # Константы приложения
│   └── utils.ts        # Утилиты
└── types/              # TypeScript типы
```

## 📦 Модели данных

### Товар (Product)

```typescript
interface IProductItem {
  id: string;
  title: string;
  description: string;
  price: number | null;
  image: string;
  category: string;
}
```

### Покупатель (Customer)

```typescript
interface ICustomerData {
  payment: "card" | "cash" | "";
  email: string;
  phone: string;
  address: string;
}
```

### Заказ (Order)

```typescript
interface IOrderData extends ICustomerData {
  items: IProductItem[];
}
```

## 🚀 Быстрый старт

### Предварительные требования

- Node.js 16+
- npm или yarn

### Установка и запуск

1. **Клонируйте репозиторий**

```bash
git clone <repository-url>
cd web-larek
```

2. **Установите зависимости**

```bash
npm install
# или
yarn install
```

3. **Настройте переменные окружения**
   Создайте файл `.env` в корне проекта:

```env
VITE_API_ORIGIN=https://larek-api.nomoreparties.co
```

4. **Запустите проект**

```bash
npm run start
# или
yarn start
```

Приложение будет доступно по адресу: `http://localhost:3000`

## 📜 Доступные команды

| Команда         | Назначение                 |
| --------------- | -------------------------- |
| `npm run start` | Запуск development-сервера |
| `npm run build` | Сборка production-версии   |
| `npm run dev`   | Запуск в режиме разработки |
| `npm run lint`  | Проверка кода линтером     |

## 🎯 Основные компоненты

### 🏷️ ProductCatalog

Управление каталогом товаров:

- Хранение списка товаров
- Поиск товаров по ID
- Управление текущим выбранным товаром

### 🛒 ShoppingCart

Работа с корзиной покупок:

- Добавление/удаление товаров
- Подсчет общей стоимости
- Проверка наличия товаров

### 👤 Customer

Управление данными покупателя:

- Сохранение контактных данных
- Валидация введенной информации
- Очистка данных после заказа

## 🔧 Базовые классы

### Component

Базовый класс для всех UI-компонентов с методами:

- `render()` - отрисовка компонента
- `setImage()` - работа с изображениями

### ApiClient

Класс для работы с API:

- `getProductList()` - получение товаров
- `createOrder()` - оформление заказа

### EventEmitter

Система событий для связи между компонентами:

- `on()` - подписка на события
- `emit()` - генерация событий

## 🌐 API Endpoints

| Метод | Endpoint       | Назначение               |
| ----- | -------------- | ------------------------ |
| GET   | `/api/product` | Получение списка товаров |
| POST  | `/api/order`   | Создание нового заказа   |

Для тестирования API используйте [Postman коллекцию](https://larek-api.nomoreparties.co/weblarek.postman.json).

## 🎨 Стилизация

Проект использует **SCSS** с модульным подходом:

- Переменные для цветов и размеров
- Миксины для повторяющихся стилей
- Модульные стили для компонентов

## 🔍 Валидация форм

Реализована комплексная валидация:

- Email формата `user@example.com`
- Телефон в российском формате
- Обязательные поля адреса и способа оплаты
- Визуальное отображение ошибок

## 📱 Адаптивность

- **Desktop** (1200px+): полная функциональность
- **Tablet** (768px-1199px): адаптированная сетка
- **Mobile** (<768px): мобильная оптимизация

## 🛠️ Технический стек

- **Frontend**: TypeScript, SCSS, HTML5
- **Build Tool**: Webpack с горячей перезагрузкой
- **Architecture**: MVP + Event-Driven
- **API**: RESTful с JSON форматом

## 🤝 Разработка

### Структура коммитов

```
feat: добавление новой функциональности
fix: исправление ошибок
docs: обновление документации
style: изменения форматирования
refactor: рефакторинг кода
```

### Рекомендации по разработке

1. Используйте TypeScript для типизации
2. Следуйте принципам MVP архитектуры
3. Тестируйте на разных устройствах
4. Проверяйте валидацию форм

## 📄 Лицензия

MIT License - подробности в файле [LICENSE](LICENSE).

---

**Разработано с ❤️ для сообщества веб-разработчиков**
